import { describe, it } from "vitest";

// TODO:
describe.todo("mustang validator", () => {
	it("should catch validation errors", async () => {});

	it("should pass valid documents", async () => {});

	it("should handle unsupported profiles gracefully", async () => {});
});
