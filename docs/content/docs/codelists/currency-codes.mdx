---
title: Currency Codes
---

import {
  CURRENCY_CODES,
  CURRENCY_CODES_IDENTIFIER,
  CURRENCY_CODES_VERSION,
} from "node-zugferd/codelist/currency-codes";

## Definition [toc]


Identifier: <span className="text-fd-muted-foreground">{CURRENCY_CODES_IDENTIFIER}</span>

Version: <span className="text-fd-muted-foreground">{CURRENCY_CODES_VERSION}</span>

<div className="w-fit">
  <Link
    href="https://www.xrepository.de/details/urn:xoev-de:kosit:codeliste:vatex_1"
    target="_blank"
  >
    Source
  </Link>
</div>

```ts
import {
  CURRENCY_CODES,
  type CurrencyCodesCode,
} from "node-zugferd/codelist/currency-codes";
```

<table>
  <thead>
    <tr>
      <th>Alphabetic Code</th>
      <th>Currency</th>
    </tr>
  </thead>
  <tbody>
    {CURRENCY_CODES.map(({ code, currency }) => (
      <tr key={code}>
        <td>
          <code>{code}</code>
        </td>
        <td>{currency}</td>
      </tr>
    ))}
  </tbody>
</table>
